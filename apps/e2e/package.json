{
  "name": "@scribe-drop/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "pnpm --filter @scribe-drop/web run build:client",
    "test": "playwright test --config playwright.config.ts",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0",
    "@types/node": "26.1.1",
    "typescript": "6.0.3"
  }
}
