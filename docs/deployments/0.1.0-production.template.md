# ScribeDrop 0.1.0 production deployment record

## 状態

- Release: `0.1.0`
- Source commit: release実施時にGitHub deploymentとtagで照合する
- Status: Not started
- production traffic: Disabled

実account、domain、resource/deployment ID、image参照、credential、署名URL、利用者data、
録音内容、文字起こし本文はこの記録へ保存しない。識別子はgit ignoredのplan/stateと
platform deployment historyだけに保持する。

## Preflight

- [ ] release branchとpackage versionが一致する
- [ ] required GitHub checksが同じcommitで成功している
- [ ] RunPod publication evidenceのimage digest、SBOM、scanが同じcommitに対応する
- [ ] Wrangler、runpodctl、Node、pnpm、uvのversionがproject固定値と一致する
- [ ] CloudflareとRunPodの認証先をread-onlyで確認した
- [ ] production planにstaging value、placeholder、secret原文がない

## Resource bootstrap

- [ ] production専用D1、private R2、Queue、DLQを作成した
- [ ] R2 CORS、lifecycle、Event Notificationをread-backした
- [ ] production専用Pages、Orchestrator origin、Access application/policyを作成した
- [ ] production専用RunPod template/endpointを固定image digestから作成した
- [ ] active workers 0、max workers 1、GPU 1、Secure Cloud、Flexを確認した
- [ ] 固定GPU候補の順序がstaging evidenceと一致し、REST read-backした
- [ ] 両GPU候補がSecure-onlyかつavailableで、第1候補High/Mediumである
- [ ] Network Volume、永続disk、FlashBootが無効である

## Secret and deployment

- [ ] Web必須secret 4件を名前だけで確認した
- [ ] Orchestrator必須secret 5件を名前だけで確認した
- [ ] RunPod templateに長期credential、token、webhookがない
- [ ] D1 migration `0001`から`0008`を順に適用し、未適用が0件である
- [ ] Orchestrator binding、Queue consumer、DLQ、5分Cron、retention値をread-backした
- [ ] Web build、Pages Functions、static assetを同じrelease commitからdeployした

## Security and end-to-end smoke

- [ ] root、`/api/me`、preflightが未認証時にAccess loginへ遷移する
- [ ] 認証後もAccess JWT、Origin、CSRF、所有権検証を迂回できない
- [ ] R2はprivateで、production exact origin以外のCORSとlist/readを拒否する
- [ ] 固定dummy mediaでuploadから3成果物、manifest-last、Discord通知まで成功する
- [ ] duplicate、cancel、page close後の復元、offline fallbackを確認した
- [ ] logにsecret、URL query、本文、メールアドレス原文がない

## Cleanup and release decision

- [ ] dummy jobを明示削除し、安全期限後にD1 rowが0件である
- [ ] cache-busted Object API listingでdummy R2 objectが0件である
- [ ] DLQ、Unhealthy/Outdated worker、未送信outbox、retention errorが0件である
- [ ] active workersを0へ戻した
- [ ] production trafficを許可するgo/no-go判断を記録した
- [ ] `main`と`develop`への`--no-ff` mergeおよび`v0.1.0` tagを確認した

## Rollback

- applicationは直前の検証済みdeploymentへ戻し、forward-only migrationは削除しない。
- RunPodは直前の検証済みtemplate/image digestへ戻し、volumeやFlashBootを有効にしない。
- resource削除は自動化せず、production専用かつ未使用であることを別reviewで確認する。
- rollback後もAccessを閉じたままread-backとcleanupを完了する。
